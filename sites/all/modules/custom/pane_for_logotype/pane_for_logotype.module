<?php

/**
 * Implements hook_ctools_plugin_directory().
 */
function pane_for_logotype_ctools_plugin_directory($owner, $plugin) {
    if ($owner == 'ctools' && $plugin == 'content_types') {
        return 'plugins/' . $plugin;
    }
}
?>
